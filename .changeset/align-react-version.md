---
"@eudi-verify/react": patch
---

Align `@eudi-verify/react` with the unified package version line. It now joins the fixed release group alongside `@eudi-verify/server`, `@eudi-verify/client`, and `@eudi-verify/embed`, so all four packages share a single version from this release onward. No API changes.

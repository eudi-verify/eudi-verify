---
description: Structured planning — contracts and checklist only; activate with @plan-mode in Plan mode
alwaysApply: false
---

# Plan Mode Controller

**Activation:** This rule applies only when explicitly `@plan-mode` in chat. Use with Cursor's native Plan mode. If Plan mode is not active, ignore this rule entirely.

Rules: see [docs/agent-plan-mode.md](../../docs/agent-plan-mode.md) — tool-agnostic, shared with Claude Code's `/plan`. Not restated here to avoid double-loading.

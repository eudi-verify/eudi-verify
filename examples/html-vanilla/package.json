{
  "name": "@eudi-verify/demo-html-vanilla",
  "version": "0.0.3",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --import tsx server.ts",
    "build": "tsc && cp -r public dist/",
    "test": "echo 'Manual testing only'"
  },
  "dependencies": {
    "@eudi-verify/server": "workspace:*",
    "@eudi-verify/embed": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.0.0"
  }
}

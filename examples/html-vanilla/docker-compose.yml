services:
  demo:
    build:
      context: ../..
      dockerfile: examples/html-vanilla/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - TOKEN_SECRET=${TOKEN_SECRET:?TOKEN_SECRET is required}
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped

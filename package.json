{
  "name": "eudi-verify",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run && pnpm --filter @eudi-verify/embed test",
    "test:unit": "vitest run",
    "test:e2e": "pnpm --filter @eudi-verify/embed test:e2e && pnpm --filter example-vue test:e2e && pnpm --filter example-react test:e2e",
    "lint:api": "spectral lint openapi/eudi-verifier.yaml",
    "build": "pnpm -r build",
    "verify": "bash scripts/ci.sh",
    "hooks:install": "bash scripts/install-git-hooks.sh"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@stoplight/spectral-cli": "^6.16.0",
    "happy-dom": "^20.8.9",
    "prettier": "^3.8.4",
    "typescript": "^5.0.0",
    "vitest": "^3.2.6"
  }
}
